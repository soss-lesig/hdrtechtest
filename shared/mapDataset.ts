// Pure mapping function - transforms a raw dataset into the flat shape the UI needs
// Handles null/undefined defensively at the boundary

// TODO: implement mapDataset(raw: RawDataset): MappedDataset
// - Extract the four fields from nested paths
// - Defend against null/missing values
// - Return flat MappedDataset object
